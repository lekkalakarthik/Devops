nname: Print variables

on:
  workflow_dispatch 

jobs:
  Print:
    runs-on: ubuntu-latest
    steps:
      - run: |
         echo ${{ secrets.API_KEY }}
         echo ${{ secrets.REPO_SEC }}
         echo ${{ env.DB_USER }}
         echo ${{ env.REPO_VAR }}
